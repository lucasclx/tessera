# backend/src/main/resources/application-dev.properties
# Este arquivo pode sobrescrever ou adicionar propriedades de application.properties
# para o perfil 'dev'

# Server Port (pode ser o mesmo ou diferente)
# server.port=8080

# MySQL Database (geralmente o mesmo para dev local)
# spring.datasource.url=jdbc:mysql://localhost:3306/tessera_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=

# JPA / Hibernate
# spring.jpa.hibernate.ddl-auto=update # 'update' é comum para dev
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Properties - Usar variável de ambiente ou um segredo de DEV (256-bit / 32 bytes / 64 hex characters for HS256)
jwt.secret=${JWT_SECRET_DEV:e8bbc656b6d4bca2b7e9f292d18082a0af114bdf2b0f8e98f9aefffad9f7e608}
# jwt.expiration=86400000 # 24h
# jwt.refresh.expiration=604800000 # 7d

# Logging
logging.level.com.backend.tessera=DEBUG 
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE # Para ver valores de parâmetros SQL

# Configurações de Email para ambiente de desenvolvimento (ex: MailDev ou MailHog)
spring.mail.host=localhost
spring.mail.port=1025 
spring.mail.username= 
spring.mail.password= spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false
# spring.mail.properties.mail.debug=true # Ativar para debug de email

# Desabilitar verificação de saúde do email se usando MailDev/MailHog que não requer autenticação
management.health.mail.enabled=false

# Forçar inicialização de dados em dev, se necessário
app.init.data=true  