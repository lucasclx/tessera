import { Routes } from '@angular/router';
// Exemplo: import { SeuOutroComponente } from './seu-outro-componente/seu-outro-componente.component';

export const routes: Routes = [
  // { path: '', component: AppComponent }, // Cuidado: AppComponent é geralmente o host, não uma rota em si.
  // { path: 'outra-pagina', component: SeuOutroComponente },
  // Deixe vazio por enquanto se o editor for a única coisa na página principal via AppComponent
];